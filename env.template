# 🚂 VIEAgent ChromaDB - Railway Environment Variables Template
# Copy these to Railway Variables section

# 🚨 SIMPLIFIED APPROACH: Hardcoded Port 8000
# Issue Fixed: "invalid value '$PORT' for '--port <PORT>': invalid digit found in string"
# Solution: Use hardcoded port 8000 instead of environment variable expansion

# 🔧 REQUIRED VARIABLES
CHROMA_HOST=0.0.0.0
CHROMA_PORT=8000
IS_PERSISTENT=1
PERSIST_DIRECTORY=/app/data
ANONYMIZED_TELEMETRY=false
CHROMA_DB_IMPL=duckdb+parquet

# 🔒 SECURITY VARIABLES (Optional)
CHROMA_SERVER_CORS_ALLOW_ORIGINS=["*"]
CHROMA_SERVER_AUTHN_PROVIDER=""
CHROMA_SERVER_AUTHZ_PROVIDER=""

# 🌐 RAILWAY NETWORKING (Auto-set by Railway)
RAILWAY_STATIC_URL=""
RAILWAY_PRIVATE_DOMAIN=""
RAILWAY_PUBLIC_DOMAIN=""

# 📝 USAGE INSTRUCTIONS:
# 1. Copy these variables to Railway Dashboard > Variables tab
# 2. Set each variable name and value
# 3. Deploy to apply changes

# 🛠️ START COMMAND OPTIONS (All simplified with hardcoded port 8000):
# Option 1 (Dockerfile): CMD ["chroma", "run", "--host", "0.0.0.0", "--port", "8000", "--path", "/app/data"]
# Option 2 (Procfile): web: chroma run --host 0.0.0.0 --port 8000 --path /app/data
# Option 3 (railway.json): "startCommand": "chroma run --host 0.0.0.0 --port 8000 --path /app/data"

# 🔍 TROUBLESHOOTING:
# - All start methods now use hardcoded port 8000
# - No environment variable expansion issues
# - Railway will handle port mapping automatically
# - Service will be accessible via Railway's public domain 